# Multi-Vendor Admin Frontend

React + Redux Toolkit + React Router + Tailwind CSS v3

## Setup

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Runs at http://localhost:5173 with API proxy to backend.
